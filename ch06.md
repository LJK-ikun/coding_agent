# ch06 · 纵深防御的安全检查

> 本章代码：`mewcode/tools/permission.py`（门卫）+ `runner.py`（接线）+ `cli.py`（询问）
> 测试：`tests/test_ch06_permission.py`（59 条）
> 验收：`checklist.md` 的 ch06 段

---

## 这一章解决什么问题

前面几章把"手"造出来了——模型能读文件、写文件、跑命令。但它**会幻觉，用户会手滑**。

所以要在"真动手"之前加一道门卫。门卫只干一件事：

> **把一次工具调用判成 `allow` / `deny` / `ask`。**

它只判，不执行。判定通过之后才轮到 runner 真去跑。

### 为什么叫"纵深防御"

因为不是一层就够。黑名单能挡已知的坏命令，但挡不住没见过的；沙箱能管住文件，但管不到命令；规则能配得准，但**你不可能把所有情况都提前写下来**。

所以一层一层叠：自动能判的自动判，判不了的往下一层交，最后一层交给人。

**关键设计：六层从上往下过，命中即返回。**

---

## 判定是怎么走的

一次工具调用进来，按这个顺序过：

```
        ┌─────────────────────────────┐
        │  模型说：我要调 read_file    │
        └──────────────┬──────────────┘
                       ↓
        ┌─────────────────────────────┐
   1    │ 危险操作黑名单                │  命中 → deny（硬底线，谁也越不过）
        └──────────────┬──────────────┘
                       ↓ 没命中
        ┌─────────────────────────────┐
   2    │ 显式规则（会话>项目>用户）     │  命中 → 按规则说的办（allow/deny/ask）
        └──────────────┬──────────────┘
                       ↓ 没命中
        ┌─────────────────────────────┐
   3    │ 路径沙箱                     │  出界 → ask
        └──────────────┬──────────────┘
                       ↓ 在界内
        ┌─────────────────────────────┐
   4    │ 档位兜底                     │  strict → ask；default/permissive → allow
        └──────────────┬──────────────┘
                       ↓
                  判定完成
```

（代码里层号的标注和这个执行顺序不是一回事，`permission.py` 头部按"你提需求的顺序"标的号，`evaluate()` 里按上图的执行顺序排。看代码时以 `evaluate()` 里的注释为准。）

判定是**纯函数式**的——除了"永久允许"要落盘，不碰网络、不碰终端。所以测试可以全离线跑。

---

## 第 1 层：档位模式

**要解决的事**：同一个程序，不同场合你想让它有不同的松紧。刚拿到别人的代码时想让它严格点，自己天天用的项目想让它别老烦你。

**所以给了三个档**（`permission.py:59`）：

```python
MODE_STRICT     = "strict"      # 紧：没被显式放行的一律问用户
MODE_DEFAULT    = "default"     # 中：沙箱内放行，沙箱外问人
MODE_PERMISSIVE = "permissive"  # 松：只留黑名单这一道底线
```

实际效果：

| 档位 | 项目目录内 | 项目目录外 |
|---|---|---|
| `strict` | ❓ 问你 | ❓ 问你 |
| `default` | ✅ 放行 | ❓ 问你（被沙箱那层拦下）|
| `permissive` | ✅ 放行 | ✅ 放行 |

**注意 `permissive` 不等于关掉安全**——`rm -rf /` 照样拦。黑名单是最底下那条硬线，三档都拦。

**档位是兜底，不是主力。** 它排在最底下，前面几层都没结论时才轮到它说话：

```python
# ── 第 1 层：档位兜底 ────────────────────────────
if self.mode == MODE_STRICT:
    return self._decide(ACTION_ASK, "严格模式：未经显式放行的调用都要用户确认", ...)
# default / permissive 走到这里都是放行。
# 差别在哪？default 会先在上一层被沙箱拦下来问；permissive 不会。
return self._decide(ACTION_ALLOW, f"{_mode_cn(self.mode)}模式：默认放行", ...)
```

### 怎么配

| 写在哪 | 代码位置 | 生效时机 |
|---|---|---|
| `mewcode.yaml` 的 `permission_mode` | `config.py:140` | 每次启动 |
| 命令行 `--mode strict` | `cli.py:157` | 这一次启动 |
| REPL 里 `/mode strict` | `cli.py:210` | 立刻 |

命令行覆盖 YAML，靠的是这一句（`cli.py:157`）：

```python
mode=mode or cfg.permission_mode     # 命令行给了就用命令行的
```

运行中切换靠的是直接改字段（`cli.py:210`）：

```python
engine.mode = want
```

门卫每次判定都现读 `self.mode`，所以改完下一次判定就是新值，不用重启。

**档位写错要在配置阶段就报**（`config.py:105`）：

```python
if self.permission_mode not in MODES:
    raise ValueError(f"permission_mode '{self.permission_mode}' unsupported; use one of {MODES}")
```

不这么干的话，你把 `permissive` 打成 `permissiv`，程序悄悄按 default 跑，你会一脸懵——"我明明设了怎么还问我"。这类问题最难查。

---

## 第 2 层：危险操作黑名单

**要解决的事**：有几个命令是"不管什么理由都不该跑"的。它们是硬底线。

`DEFAULT_DENY_PATTERNS` 是一组正则，拦这些：

- 破坏性删除：`rm -rf /`、`rm -rf ~`、`rm -rf $HOME`
- 下载即执行：`curl ... | bash`、`wget ... | python`
- fork 炸弹：`:(){ :|:& };:`
- 写裸设备：`dd of=/dev/sda`、`> /dev/sda`、`mkfs.ext4`
- 系统级破坏：`chmod -R 777 /`、`shutdown`、`reboot`

### 为什么它排在规则**之前**

因为它是"硬底线"：

```python
# 放在规则之前，是因为它是"硬底线"：用户即便写了 allow run_command *，
# 也不该让 rm -rf / 溜过去。底线就是拿来兜住误配置的。
```

规则是你写的，你可能写错、写宽了。黑名单兜住这个。

### 两道归一化，挡低成本变形

模型（或用户）不会老老实实写 `rm -rf /`。所以先做两道处理：

**① 归一化**（`normalize_command`）：去掉引号、把连续空白压成一个空格。

```
r''m   -rf   /     →     rm -rf /
```

**② 拆链**（`split_subcommands`）：按 `&&` / `||` / `;` / `|` / 换行拆开，逐段检查。

```
echo hi && rm -rf /   →   ['echo hi', 'rm -rf /']
```

拆开之后 `rm -rf /` 就藏不住了。

两道处理之后还要**再拿整条命令兜一次**——因为 fork 炸弹那种 `:(){ :|:& };:` 拆开就散架了，得整条看才认得出来。

### 也别误伤

`rm -rf ./build`、`rm -rf /tmp/mewcode-scratch` 这些**正常操作必须放行**。所以正则写的时候要盯住"目标是不是根/家目录"，而不是见到 `rm -rf` 就拦。

（写这段时踩过一个坑：最初的 `rm` 正则匹配不到 `rm -rf /*`，因为星号的位置没覆盖到。是测试里那条 `rm  -rf  /*` 把它逼出来的。）

### 只能加严

`mewcode.yaml` 里可以配 `extra_deny_patterns` 追加自己的黑名单，但**只能加严，删不掉内置的**——它是从内置表后面接上去的，不是替换。

---

## 第 3 层：路径沙箱

**要解决的事**：模型读写文件，只该在你指定的目录里活动。

默认沙箱就是**启动时所在的项目根**。也可以通过 `sandbox_roots` 配多个。

判定函数 `_in_sandbox`（`permission.py:513`）三步，每步挡一类绕过：

**① 用 `realpath` 而不是 `abspath`**

`abspath` 只展开 `..`，**不解软链接**。

假设 `D:/proj/link` 是个软链接，指向 `C:/Windows`。模型写 `D:/proj/link/x`，字符串看着还在项目内——`abspath` 也这么认为。但 `realpath` 会算出真实落点是 `C:/Windows/x`，**在项目外**。

`..` 逃逸同理，也在这一步被展开到根之外：

```
../../etc/passwd  →  D:\etc\passwd  →  出界
```

（软链接不是 Windows 快捷方式。快捷方式是个 `.lnk` 文件，你得双击它才跳转；软链接是文件系统层面的，程序按普通路径访问就会自动落到真实位置。）

**② 用 `normcase` 统一大小写**

Windows 路径大小写不敏感，`d:/proj` 和 `D:/proj` 是同一处。不统一大小写，就会把沙箱**内**的路径误判成出界。

**③ 比对时带上 `os.sep`**

只写 `startswith(root)` 的话：

```
D:/proj-secret/x   被误判成在   D:/proj   里
```

只是前缀字母相同而已。得用 `root + os.sep` 才是真正的"在其下"：

```python
target = os.path.normcase(os.path.realpath(subject.resolved))
for root in self.roots:
    root_n = os.path.normcase(os.path.realpath(root))
    if target == root_n or target.startswith(root_n + os.sep):
        return True
return False
```

（`target == root_n` 那一支照顾的是"就是根本身"这种情况。）

### 出界了怎么办

**不是拒绝，是 `ask`**（在 `default` 档下）：

```python
if sandboxed and subject.kind == "path" and not self._in_sandbox(subject):
    return self._decide(ACTION_ASK, f"路径超出沙箱范围 (允许: {...})", ...)
```

因为"写到项目外"不一定是坏事——你可能就是想让它写到别处。所以交回给你决定，而不是一刀切拒绝。

`permissive` 档跳过整层沙箱，那是用户明确选出来的取舍。

### 命令的执行目录也算

不然模型可以把 `cwd` 挪到外面再动手：

```python
cwd_param = _CWD_PARAM.get(tool_name) if sandboxed else None
if cwd_param:
    cwd_subject = make_subject("read_file", {"path": cwd_raw}, self.base_dir)
    if not self._in_sandbox(cwd_subject):
        return self._decide(ACTION_ASK, f"执行目录超出沙箱范围: ...", ...)
```

---

## 第 4 层 + 第 6 层：显式规则与优先级

**要解决的事**：给你一个"按工具 + 参数/路径模式"声明放行还是拦截的地方。

### 一条规则长什么样

```yaml
rules:
  - tool: run_command
    match: "git status*"
    action: allow
  - tool: write_file
    match: "**/*.env"
    action: deny
```

三个字段：

| 字段 | 意思 |
|---|---|
| `tool` | 工具名，支持 glob（`*` = 所有工具）|
| `match` | 参数/路径模式，支持 glob；以 `re:` 开头则按正则 |
| `action` | `allow` / `deny` / `ask` |

代码里就是一个盒子（`permission.py:288`）：

```python
@dataclass(frozen=True)
class Rule:
    tool: str = "*"
    match: str = "*"
    action: str = ACTION_ASK
    source: str = SOURCE_PROJECT
    note: str = ""
```

`frozen=True` = 盒子做好后不许改，规则是只读的。`= "*"` 是默认值，YAML 里没写就用它。

### 它管不管这次调用（`Rule.matches`）

```python
def matches(self, tool_name, subject) -> bool:
    if not fnmatch(tool_name, self.tool):   # ① 工具名对得上吗
        return False
    if self.match in ("", "*"):             # ② 模式写的是"任意" → 算命中
        return True
    if self.match.startswith("re:"):        # ③ 正则模式
        try:
            rx = re.compile(self.match[3:])
        except re.error:                    #    正则写错 → 这条当不存在，别崩
            return False
        return any(rx.search(c) for c in subject.candidates())
    return any(fnmatch(c, self.match) for c in subject.candidates())   # ③ glob 模式
```

三关，任一关不过就返回"不管"。

最后那句 `any(...)` 稍微绕：`subject.candidates()` 给出这个路径的**几种写法**（相对项目根的写法、完整路径），`any` = 只要有一种对上就算命中。这样规则里写 `src/**` 能匹配上，写 `D:\Project\my_code\src\**` 也能匹配上。

**正则写错时该规则当不存在，绝不抛异常**——门卫自己不能崩。

### 规则写在哪

两个文件：

```
<项目根>/.mewcode/permissions.yaml     ← 项目级
~/.mewcode/permissions.yaml            ← 用户全局
```

（Windows 上 `~` 就是 `C:\Users\lkj666`。）

读文件是防御式的（`load_rules_file`）：文件不存在 / YAML 写坏 / `rules` 不是列表 / 某条缺字段 —— 一律当"没有这条"，绝不抛穿。

### 优先级：会话 > 项目 > 用户

**实现只有一行**（`_ordered_rules`）：

```python
return self._session_rules + self._project_rules + self._user_rules
```

`+` 把三个列表接成一长条：

```
[A]  +  [B, C]  +  [D]   →   [A, B, C, D]
会话     项目      用户         ↑ 会话在最前，最后问
```

判定时按这个顺序挨个问：

```python
for rule in self._ordered_rules():
    if rule.matches(tool_name, subject):
        return self._decide(rule.action, ...)    # 谁先被问到，就听谁的
```

**`return` 一去，后面的就不看了。**

所以优先级不是"算权重"，是**排队 + 先到先得**。

**为什么会话级排最前**：那是你**刚刚亲口说的**，不该被磁盘上写死的旧规矩压过。

```python
# 会话级整体优先于项目级/用户级——"我刚才明确说了这次可以"应该压过
# 磁盘上写死的旧规矩。HITL 的"本会话允许"就是走这条路。
```

---

## 第 5 层：人在回路（HITL）

**要解决的事**：上面几层都有个共同前提——**它们认识这次调用**。黑名单认得这个命令，规则里写过，沙箱能判。

但总会有它们都不认识的情况。这时候不能自动放行（那前面几层就白搭了），只能**停下来问人**。

所以 HITL 不是一层筛子，是**兜底**：

```
能自动判的   →  自动判
判不了的     →  问人（HITL）
连人都问不到 →  拒绝
```

### 谁来问

判定是 `ask` 之后，runner 里这段接手（`runner.py:128`）：

```python
async def _gate(self, call) -> Optional[ToolCallResult]:
    decision = self._guard.evaluate(call.name, call.input)   # ① 先判定

    if decision.action == ACTION_DENY:                       # ② 直接拦
        return _blocked_result(call, decision.reason)

    if decision.action == ACTION_ASK:                        # ③ 要问人
        if self._ask is None:                                #    没人可问？
            return _blocked_result(call, "...（当前无可交互的确认通道）")
        scope = await self._ask(decision.request)             # ★ 问
        if scope == GRANT_DENY:
            return _blocked_result(call, f"用户拒绝了这次调用（{decision.reason}）")
        self._guard.remember(scope, decision.request)         # ④ 记账
    return None                                               # ⑤ 放行
```

关键点：**runner 不自己实现"怎么问"，而是留个口子**（`ask` 参数）。

- 控制台版传 `_ask_permission`（敲键盘问）
- 测试版传个假的（直接返回"拒绝"）

**`if self._ask is None` 那句是 fail-closed**：没人可问就拒绝。宁可不动手，也不在无人值守时悄悄放行。

### 问的样子

`cli.py:67` 的 `_ask_permission`：

```python
async def _ask_permission(req: PermissionRequest) -> str:
    print(f"\n{_YELLOW}[权限确认]{_RESET}")
    print(req.describe())          # 工具 / 命令或路径 / 为什么被拦
    print(f"  当前档位: {req.mode}")
    print(f"  若允许，将记下规则: {req.tool}  {req.suggest_match()}  -> allow")
    print(_ASK_MENU, end="")
    try:
        answer = (await asyncio.to_thread(input, "选择 [1/2/3/4]（直接回车 = 拒绝）: ")).strip()
    except (EOFError, KeyboardInterrupt):
        return GRANT_DENY
    return _ASK_CHOICES.get(answer, GRANT_DENY)
```

**关键 API 是 `await asyncio.to_thread(input, ...)`**，两个原因：

- `input()` 是阻塞的：不敲回车它不返回。
- 我们的程序是异步的（模型在流式吐字、工具在跑）。直接调 `input()` 会把整条事件循环卡死，界面僵住。
- `asyncio.to_thread(函数, 参数)` 把 `input` 丢到另一个线程去跑，主循环照常转，等它有结果了再 `await` 回来。

**任何异常输入都当拒绝**：

```python
except (EOFError, KeyboardInterrupt):   # Ctrl+D / Ctrl+C
    return GRANT_DENY
return _ASK_CHOICES.get(answer, GRANT_DENY)    # 乱敲 = 拒绝
```

`_ASK_CHOICES = {"1": GRANT_ONCE, "2": GRANT_SESSION, "3": GRANT_ALWAYS}`，用 `.get(键, 默认值)`——字典里没这个键就返回默认值。所以乱敲不会被当成"程序出错"，而是**故意设计成拒绝**。

### 答完之后：三种"记住"

`remember`（`permission.py:623`）三个分支：

```python
if scope == GRANT_ONCE:
    return None                                    # ① 什么都不记
if scope == GRANT_SESSION:
    return self.add_session_rule(...)              # ② 记进内存
# always：落盘 + 进内存，两条腿都要有
rule = Rule(..., source=SOURCE_PROJECT, ...)
try:
    save_rule(self.project_file, rule)             # ③ 写文件
except OSError:
    ...降级为会话级...                              #    盘写不进去别崩
self._project_rules.append(rule)                   #    也进内存
return rule
```

三个选项的区别：

| 你答的 | 记到哪 | 下次还问吗 | 重启还在吗 |
|---|---|---|---|
| `once` | 哪都不记 | **还问** | 不在 |
| `session` | 内存列表 | 不问了 | **没了** |
| `always` | 文件 + 内存 | 不问了 | **还在** |

`always` 那个"文件 + 内存"是重点：**文件管下次启动，内存管这次运行**，缺一个都不行。

（文件写的是 `self.project_file`，也就是项目根下的 `.mewcode/permissions.yaml`。所以"永久允许"是落在项目里，不是全局。）

**那个 `except OSError` 是兜底**：万一目录只读、权限不够，盘写不进去，不该让整条会话崩。降级成"本会话"，这次还能用，只是下次启动就没了。

### 授权前让你看见会记下什么

```python
print(f"  若允许，将记下规则: {req.tool}  {req.suggest_match()}  -> allow")
```

`suggest_match()` 决定记下来的规则长什么样：

- **路径类**：精确到这次的路径
- **命令类**：取"首个词 + `*`"——所以放行 `pytest -q a` 之后，`pytest b` 也不会再问

透明是授权的前提：你得先知道它要记什么，才好决定给不给。

---

## 被拦下之后会怎样

**不会中断整轮对话。**

被拦时返回的是一条 `is_error=True` 的工具结果，内容是"被哪条规则拒了"：

```python
def _blocked_result(call, reason) -> ToolCallResult:
    ...  # 装成一条失败收据
```

这条结果照常回灌给模型。模型看到"这条路被堵了"，可以改道把活干完——而不是整轮崩掉。

**实测过的一条：** 让它往沙箱外的绝对路径写文件，门卫弹 `[权限确认]`；答 `4`（拒绝）；模型收到"权限拦截：用户拒绝了这次调用"之后，**没有试图用 shell 命令绕过去**，而是回头问用户希望怎么办。事后确认那个文件没落盘。

---

## 六层速查

| # | 层 | 做什么 | 不通过时 |
|---|---|---|---|
| 1 | 档位模式 | `strict`/`default`/`permissive` 定基调 | 兜底决定 |
| 2 | 危险操作黑名单 | `rm -rf /`、`curl \| bash`、fork 炸弹… | 直接拒绝 |
| 3 | 路径沙箱 | 文件工具 + `run_command` 的 `cwd` 只能在允许目录内 | 交给用户 |
| 4 | 显式规则 | `工具 + 参数/路径模式 → allow/deny/ask` | 按规则执行 |
| 5 | 人在回路 | 前几层没结论时问用户 | 本次/本会话/永久 或 拒绝 |
| 6 | 规则优先级 | 会话级 > 项目级 > 用户全局 | 贯穿 2、4 层 |

---

## 诚实的边界

**黑名单是网，不是墙。**

它会剥引号、拆 shell 链、压空白，来挡低成本的变形。但它**不承诺对抗刻意规避**——不做 shell 语法树解析，不做 base64/变量拼接还原。

它的价值在于**兜住模型幻觉与用户手滑**，而不是对抗一个决意作恶的对手。

同理，本章是**应用层判定，不是内核级隔离**——没有容器、没有 namespace、没有只读挂载。真要跑不可信的代码，得上进程级隔离，那是另一个量级的事。

还有一条：**每次决策的审计日志**没做，留痕归后续章节。

---

## 这一章没动 `agent.py`

门卫挂在 runner 上，而 `Agent` 本来就接受外部传入的 runner，所以"大脑"那一层一行不用改。

这是 ch03 定下 `runner` 可注入的好处——加一层防护不用动上层。

---

## 怎么用

```bash
# 配置（都可省略，省略 = 默认档 + 项目根沙箱 + 内置黑名单）
permission_mode: default
sandbox_roots: ["D:/Project/my_code", "D:/shared"]
extra_deny_patterns: ["\\bgit\\s+push\\s+--force\\b"]

# 命令行临时覆盖档位
python -m mewcode --mode strict
```

REPL 里的命令：

```
/mode               看当前档位
/mode strict        切档（立刻生效）
/permissions        摊开当前生效的护栏
```

---

## 测试

```bash
python -m pytest      # 109 条：旧 50 + ch06 新 59
```

ch06 的 59 条全离线——HITL 用假的 async 回调代替键盘，不碰真实终端。

分组：

1. 黑名单（拦得住 + 不误伤 + 只能加严 + 压过 allow 规则）
2. 沙箱（内外 / `..` 逃逸 / cwd / 额外 root）
3. 规则与三层优先级
4. 三档兜底
5. HITL 四种答复与执行器集成
6. 规则落盘与读回
