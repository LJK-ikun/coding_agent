# MewCode — spec.md（活文档）

> 增量维护：每次需求说一点，改一点。不删。
> 根目录 `D:\Project\my_code`；包名 `mewcode`。

## 背景与路线图

MewCode 是一个用 Python 写的、跑在终端的 agent 框架（对标 ClaudeCode 这类 CodingAgent）。
上层模块包括：AgentLoop、TUI、SubAgent、Skill、Team。整个框架分成多章推进（ch03 起做工具循环，ch08 做上下文 Compact 等）。

**当前章节 = ch02：让 AI 开口说话（LLM 传输层）**。
它的作用：让上层（AgentLoop/TUI/SubAgent/...）只需拿一个统一接口和 LLM 收发，不必各自面对 SSE 流、Thinking 签名回传、两家 Provider 的消息差异。

## 交付物（本 ch02 范围）

- 统一 `LLMClient`（异步流式接口）+ 两个内置实现：Anthropic、OpenAI。
- `ConversationManager`：两层消息模型。内部一份通用 `Message`（可承载 thinking / tool_use / tool_result），对外序列化到具体 Provider 的请求体，并把各家流事件归一化成统一的流事件。
- 落点：`mewcode/client.py`、`mewcode/conversation.py`。

## 功能需求

- F1：`LLMClient` 是对上层唯一的抽象。它暴露 `stream(...)`（异步、返回流式事件迭代器）作为核心；并有一个非抽象的"设置最大输出 token 数"方法。
- （F2–Fn 由你逐条喂，我再逐条补进这一节。当前留待对齐，不猜。）
- 上层（AgentLoop、TUI 装配点、SubAgent、Compact、Skill）只依赖一个 `LLMClient` 即可运行，不触碰 SSE 细节。

## 明确不做（本 ch02 Out of Scope）

- **执行工具**：真去调工具、把结果喂回模型的那个 agent 循环——归 ch03，不属于 ch02。ch02 只负责让消息/流能表达工具的"形态"。
- 上下文裁剪/Compact——归 ch08。
- SubAgent/Team/Skill 的编排逻辑——它们的接口由 ch02 支持，但实现不在这章。

## 关键设计约束（暂定，可改）

- 两种后端都走 **SSE 流式**：边生成边吐，不等待全部生成完。
- 支持 Claude 的 **extended thinking**。
- 配置：YAML，四个核心字段 `protocol` / `model` / `base_url` / `api_key`，`protocol` 决定走哪家。
- Provider 层抽象成统一接口，以后加新后端只增实现不改上层。

> 具体函数签名、参数默认值、错误文本、事件枚举名、token 上限等**实现值**一律不进 spec，统一收进 `checklist.md`。

---

# ch03：让模型能"动手"（工具系统）

## 背景

ch02 让 AI "开口说话"（传输层）。这一章给模型装上"手"：当用户提的问题需要读/写/改文件、跑命令、搜代码时，模型不再只回文字，而是**点名要调某个工具**；我们（程序）真去执行，把结果回灌进对话历史，模型据此决定下一步。

## 当前章节 = ch03：工具系统（单发）

目标是"从只会说话的聊天机器人 → 能真正干活的 Agent"的第一步。

## 能力清单（一句话一条）

- 统一工具接口：每个工具都带"名字 / 一句话说明 / 参数说明书 / 执行方法"，模型据此点名，程序据此执行。
- 六个内置工具：读文件、整写文件、改文件（唯一匹配替换）、跑 shell 命令、按通配符找文件、按正则搜代码内容。
- 工具注册中心：集中登记、按名查找、一键导出成底层 API 认得的工具清单（发请求时随带，让模型知道有哪些手）。
- 工具执行带超时与兜错：单次调用超时即终止；任何意外都包成"失败结果"还给模型，而非让整条会话崩掉。
- 流式工具调用解析：客户端把模型"流式吐出来"的工具参数碎片拼成完整 JSON。
- 结果回灌历史：执行完，把每个工具结果配对回原调用，作为一条 user 侧消息写回对话历史，供模型下次看到。
- 改文件安全：只在原文**恰好唯一匹配**一处时才动手；0 处或多处都给出能让模型重试的明确报错。
- 相对路径有锚点：六个工具解析相对路径时统一基于一个工作根目录，避免"在哪跑"成悬案。
- 在终端主流程里真能演示：用户让模型去读/写/跑，能亲眼看到工具动手并打印结果。

## 明确不做（本 ch03 Out of Scope）

- **多轮自动循环（AgentLoop）**：模型拿到一次工具结果就停，不再自动拿结果去追问模型让它继续调工具——归下一章。
- **命令执行前的用户确认门卫**：模型想跑命令就跑（安全性取舍留待后续章节）。
- SubAgent / Skill / Team 等编排、上下文 Compact——同前，不在本章。

## 关键设计约束（沿用 ch02）

- 工具结果在协议层面算作 user 那一侧的消息块回传。
- 无工具消息的序列化输出与 ch02 保持一致，纯聊天不受影响。
- 工具内部重活（读盘、递归、子进程）不阻塞流式事件循环。

> 实现值（默认超时秒数、六工具名字、错误文案、忽略目录名单等）统一收进 `checklist.md`。

---

# ch05：指令工程（稳定/易变分流 + prompt 缓存）

## 背景

ch04 让模型"自己反复动手"。但到此为止，系统几乎不喂任何指令：`--system` 默认空、环境信息（cwd/时间/git）从未进过请求。模型像被蒙眼空投。这一章做"指令层"：把该写的指令写对、把会变的环境搬开、再用缓存命中数字证明"稳定前缀省了 token"。

## 能力清单（一句话一条）

- 指令拆成模块（身份/工具纪律/输出风格…），带 `priority` 按序装配成**稳定 system**，后续插模块只增不改。
- 环境信息（cwd/platform/时间/git）用 `collect_env()` 独立收集，**不进稳定 system**——它是会变的，进了缓存前缀必 miss。
- Anthropic `prompt_caching: true` 时，在稳定 system 文本块与**末个工具**上打 `cache_control` 断点，缓存"system+全部工具"这段稳定前缀。
- env 作为**首条临时 user 消息**随每轮请求送出（在缓存断点之后、不写回正式历史），变化不冲缓存。
- `StreamEnd` 新增缓存计量字段；cli 在命中时打印 `[cache] 读 X / 写 Y`，让缓存是否生效变成一个看得见的数字。

## 明确不做（本章 Out of Scope）

- **运行时注入带标签消息 / 按轮开关（如 plan mode）**：涉及消息层对"非用户消息"的建模，与 `agent.py` 里延后的 PlanMode 状态机属后续章，本章只把 env 的对话通道先打通。

## 关键设计约束

- 缓存走 anthropic `cache_control`；openai 后端无此机制，忽略 `prompt_caching` 标志（`instructions` 仍纯字符串）。
- `prompt_caching` 默认关，保证 ch02/ch03 的"无缓存 body 逐字节相等"断言不回归。
- env 由 Agent 在调用前临时拼进发给 client 的消息列表，**不调用 `cm.add_user`**——正式历史里始终不含环境快照。

> 实现值（断点位置、模块列表、字段默认值）统一收进 `checklist.md`。

---

# ch06：给"手"装上护栏（纵深防御的安全检查）

## 背景

前五章让模型能开口、能动手、能自己反复动手，还写好了指令。可这只手一直是**不设防**的：模型想 `rm -rf /` 就真跑，想写 `/etc/passwd` 就真写。模型会幻觉、会误解指令，用户也可能只是打错一个字。这一章在"真执行"之前加一道门卫。

## 当前章节 = ch06：纵深防御的安全检查

不是"一道锁"，是**六层叠着来**——一层被绕过，下一层还兜着。这也是本章取名的由来。

## 能力清单（一句话一条）

- **危险操作黑名单**：执行前拦掉已知的不可逆破坏（递归强删根/家目录、远程脚本下载即执行、fork 炸弹、写块设备、格式化、关机重启…）。任何档位都拦，且优先于用户自己写的放行规则——底线就是拿来兜住误配置的。
- **命令归一化**：比对前先剥引号、拆 shell 链（`&&`/`||`/`;`/`|`）、压空白，让 `r''m -rf /`、`echo hi && rm -rf /` 这类低成本变形现出原形。**不承诺对抗刻意规避**——它是网，不是墙。
- **路径沙箱**：读/写/改/找/搜五个路径类工具，加上 `run_command` 的 `cwd`，一律只能落在允许的根目录范围内。判定走 `realpath` 归一，`../` 序列与软链接都逃不出去。
- **可配置的允许/拒绝/询问规则**：按「工具 + 参数或路径模式 → allow/deny/ask」声明。`match` 用 glob（路径与命令同一套写法），需要精确控制时写 `re:` 前缀走正则。
- **多档权限模式**：`strict` / `default` / `permissive` 三档作为**最底层的兜底**，覆盖在具体规则之下。命令行 `--mode` 或 REPL 里 `/mode` 可随时切。
- **人在回路（HITL）**：前几层都没给出结论时，把决定权交回用户，四选一——本次允许 / 本会话允许 / 永久允许 / 拒绝；并且**把"允许之后会记下哪条规则"先摆给用户看**，让授权是知情的。
- **规则优先级**：会话级临时规则 > 项目级规则文件 > 用户全局规则文件。同层内先声明者胜。
- **门卫落点唯一**：判定挂在 `ToolRunner` 执行之前——那是"模型的手"唯一的出口，绕不过去；新增工具自动受保护。
- **拒绝是一条可读的信息**：被拦下不是抛异常终止整轮，而是回一条失败结果告诉模型"被哪条规则拒了、请换做法"。模型因此能改道把活干完。

## 明确不做（本章 Out of Scope）

- **审计日志**（把每次决策落盘留痕）——后续章节。
- **对抗刻意规避**：不做 shell 语法树解析、不做 base64/变量拼接还原。本章的威胁模型是"模型幻觉 + 用户手滑"，不是"决意作恶的对手"。
- **沙箱的进程级隔离**（容器/namespace/只读挂载）——本章是应用层判定，不是内核级隔离。
- **网络访问管控**——不在本章。
- SubAgent / Skill / Team 编排、上下文 Compact——同前，不在这章。

## 关键设计约束

- **不挂门卫 = 老行为逐字节不变**：`ToolRunner(registry)` 不带 `guard` 时，ch03–ch05 的行为与测试全部保持原样。
- **判定与交互分离**：`permission.py` 只负责"判"（纯逻辑、可脱机单测），"问用户"由 `runner` 通过注入的异步回调完成——执行器不碰键盘，UI 才有权决定怎么问。
- **fail-closed**：判定为"要问"却没有可交互的确认通道时，按拒绝处理。宁可不动手，也不在无人值守时悄悄放行。
- **黑名单只可加严不可放松**：YAML 里的 `extra_deny_patterns` 只能追加，删不掉内置的那些。
- **规则文件只在需要时才读写**：文件不存在/写坏了 = 没有规则，绝不因此让启动崩掉；"永久允许"落盘失败时降级为会话级，不中断会话。

> 实现值（默认黑名单模式、三档的兜底语义、规则匹配写法、规则文件路径、询问选项文案）统一收进 `checklist.md`。

---

# ch09：让 MewCode 有记忆（指令文件 / 会话存档 / 自动笔记）

## 背景

前八章：能对话、能动手、能自己反复动手、有护栏、有上下文压缩。但每次开会话它都**从零开始**——不知道这个项目有什么约定，不记得上次聊到哪，也不会把用户的偏好记下来。这一章补"记忆"。

分四块推进：**① 指令文件 ② 会话存档 ③ 会话恢复 ④ 自动笔记**。

## ① 指令文件（已完成）

### 能力清单

- **两层**：用户级 `~/.mewcode/MEWCODE.md`（跨项目都算数）+ 项目级 `<工作目录>/MEWCODE.md`（只对本项目）。**项目级排前面**——越具体越优先。
- **`@include`**：指令里写一行 `@include 别的文件.md`，把那份正文拉进来。最多嵌套 3 层；不许跳出这份指令文件所在的目录；找不到或越界都**原样留着那行**，不报错。
- **装配位置**：拼进稳定 system 末尾（`priority=100`），**启动只读一次**——会话中途改文件不重读。
- **可见性**：启动横幅报一句"指令: 已加载（N 字符）"，让用户知道到底生效没有。

### 明确不做（本章）

- **热重载**：会话中途改指令立刻生效。稳定前缀必须逐字节不变——ch05 的缓存全靠它。
- **读取失败告警**：编码坏了 / 权限不够时静默当"没有"，用户只能从横幅消失间接发现。后续可补。

### 关键设计约束

- `instructions.py` 只负责"把文件读成一段文本"，不碰 system 装配、不碰缓存——它不联网不写盘，离线测试秒跑。

## ② 会话存档（已完成）

### 能力清单

- **一个会话 = 一个 `.mewcode/sessions/<id>.jsonl`**，一行一条消息。ID 形如 `20260914-213045-a1b2c3`（时间 + 随机尾巴，同秒也不撞）。
- **追加写**：只往末尾写，不碰老数据。追加一条 O(1)；写一半崩了最多丢最后半行，前面全好。
- **会话名片**：每个会话另存一份 `<id>.meta.json`（ID / 标题 / 消息数 / 更新时间）。**列会话列表只扫这些几百字节的小文件**，不用把每个 JSONL 整个读一遍。
- **标题零成本**：取第一条用户消息的第一行，截 40 字，不花 LLM。
- **两条命令**：`/sessions` 列最近 10 条；`--continue` 接最近一次、`--resume <ID>` 接指定那次。

### 明确不做（本章）

- **自动标题摘要**：让 LLM 读一遍会话起个更好的标题。多一次调用、多一处异步，先不换。
- **按会话搜内容**：`/sessions` 只列名片，不做全文检索。要搜直接 grep 那些 JSONL。

### 关键设计约束

- 追加写的对手是"整个文件是一个大 JSON 数组"——那样每加一条都要重写全文，写一半崩了**全废**。日志格式换来的是"最坏只丢最后半行"。
- 落盘时机跟着主循环走：用户这句先落（后面调模型崩了也留得住），这一轮的问和答再落一次。**绝不攒到退出再写**——退出路径上崩了就全没了。
- meta 必须**原子替换**（先写 `.tmp` 再 rename）：它是整份重写的，写到一半崩了留下半截 JSON，会让整个会话列表读不出来。

## ③ 会话恢复（已完成）

### 能力清单

- **坏行跳过**：JSON 坏了 / 缺 `role` 的行跳过继续，启动时如实报"有 N 行读不动"。空行不算坏。
- **悬空调用截断**：`tool_use` 喊了却等不到 `tool_result`（崩溃正在工具执行中），从那条起整条丢掉，恢复到最后一对完整的地方，报"尾部 N 条已截断"。
- **压缩标记回放**：存档里的 `{"type":"compact","summary":...}` 行，回放时**把前面攒的消息整段丢掉、换成这份纪要**——否则恢复出来的历史会比退出时更长，等于上次那次压缩白压。
- **恢复时先量一次**：回放完若已超阈值，先压一次再进 REPL，别第一轮就超限。
- **久别提醒**：距上次活跃超过 30 分钟，插入一条 `[提示] 距上次对话已经过了 N 小时`。只提醒一次。

### 明确不做（本章）

- **多会话并行写**：两个进程接同一个会话 ID 会互相追加。单用户在单终端用，先不管。
- **恢复时重放最后一次工具调用**：重跑工具可能有副作用（写文件、跑命令）。宁可截断。

### 关键设计约束

- 悬空截断不是"清理垃圾"，是**给模型一个能接得上的历史**——它看到"我说要调工具，然后没下文"会以为工具还在跑。
- 久别提醒和笔记回注都走 ch05 那条**临时 user 消息**通道：不写回历史、不进缓存前缀，顺带白拿"改了就下轮生效"。

## ④ 自动笔记（已完成）

### 能力清单

- **两级四类**：用户级 `~/.mewcode/memory.md`（用户偏好 / 纠正反馈，跟着人走）+ 项目级 `<工作目录>/.mewcode/memory.md`（项目知识 / 参考资料，跟着项目走）。
- **何时更新**：每 5 轮整理一次；退出时再整理一次（尽力而为）。
- **更新方式**：把两份现有笔记 + 最近 20 条对话一起喂给 LLM，让它**整份重写**、按固定四格输出。
- **回注**：每轮重读两个文件拼成一段，随请求送出去，开口声明"这不是用户刚说的话"。
- **三条命令**：`/memory` 看、`/memory clear user|project` 清空、`/memory edit` 报路径。

### 明确不做（本章）

- **相似度算法去重**："这两条是不是一回事"本身就是语言判断，写算法不如直接问它。
- **笔记容量上限 / 自动淘汰**：靠提示词让它合并和删过期的，不做硬性条数限制。
- **退出时保证写完**：Ctrl+C 打断时来不及。每 5 轮那次才是真正的安全网。

### 关键设计约束

- **绝不用空内容盖掉旧笔记**：模型没照格式回、或一个字没吐，就返回 False、原样留着——宁可这次没更上。
- **笔记更新失败不能带走会话**：整段包 try/except。它是"顺手做的事"，做不成也不能让用户对着崩掉的 REPL。
- **去重交给 LLM，但走"整份重写"路线**：不做增量 diff——LLM 看得到全貌才好合并。
- **只写不读的两个分支互不干扰**：用户级那格写用户级文件，项目级那格写项目级文件，一格串不到另一格去。
